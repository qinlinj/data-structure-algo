package org.qinlinj.algoframework._1_core_framework._3_sliding_window._4_longest_substring_without_repeating;

import java.util.*;

// @formatter:off
public class LengthOfLongestSubstring {
    public int lengthOfLongestSubstring(String s) {
        // Map to track character frequencies in current window
        Map<Character, Integer> window = new HashMap<>();

        // Initialize pointers and result
        int left = 0, right = 0;
        int res = 0; // Length of longest substring found

        // Expand the window by moving right pointer
        
        return res;
    }
}
